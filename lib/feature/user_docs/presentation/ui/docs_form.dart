part of 'docs_page.dart';

class _UserDocsForm extends StatelessWidget {
  const _UserDocsForm();

  @override
  Widget build(BuildContext context) =>
      const SingleChildScrollView(child: Text('User Docs'));
}
