part of 'sign_in_page.dart';

class _SignInForm extends StatelessWidget {
  const _SignInForm();

  @override
  Widget build(BuildContext context) =>
      const Center(child: Text('Sign In Form'));
}
